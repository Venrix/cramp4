import 'package:flutter/material.dart';

class TrimScreen extends StatelessWidget {
  const TrimScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Trim — Coming Soon',
        style: TextStyle(color: Colors.white38, fontSize: 16),
      ),
    );
  }
}
